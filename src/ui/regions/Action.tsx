// Action region — controls bar at the bottom (spin, bet ±, auto-spin; STAGE-003).
// Presentational placeholder; reserves ≥44px for touch targets (constraint: touch-targets-44).
export default function Action() {
  return (
    <section className="cabinet__action" aria-label="Controls">
      {/* Spin / bet / auto-spin controls land here in STAGE-003 */}
    </section>
  );
}
