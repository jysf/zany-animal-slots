// Build-time constants injected by Vite `define` (see vite.config.ts).
// Surfaced in the Paytable → About section so the live build is identifiable.
declare const __APP_VERSION__: string; // semver from package.json, e.g. "1.0.0"
declare const __BUILD_SHA__: string; // short git commit, e.g. "f467311"
declare const __BUILD_DATE__: string; // commit date, e.g. "2026-07-03"
