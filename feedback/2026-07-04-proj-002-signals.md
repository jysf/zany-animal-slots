# PROJ-002 framework-feedback signals — structured triage

Running signal set for **PROJ-002 (Machines & Metrics)**, companion to the PROJ-001
set ([2026-07-03-proj-001-signals.md](2026-07-03-proj-001-signals.md)). For the
**template author**, not this project. Same buckets and conventions as the PROJ-001
file: `lesson` · `process-debt` · `product` · `risk`; `disposition_at` is stage-close
for lessons, project-close otherwise. Nothing here is adopted or rejected yet — triage
input only.

```yaml
signals:
  # ─────────────────────────── PROCESS-DEBT (tooling/framework fixes) ───────────────────────────
  - id: active-project-resolver-status-blind
    type: process-debt
    summary: "get_active_project resolves the 'active project' as the lowest-numbered project directory, ignoring the project's status field — so once a second project exists, status/cost-audit/specs-by-cycle silently report the FIRST (shipped) project even when a later one is the active wave."
    evidence: "PROJ-002 STAGE-007 kickoff + SPEC-038/039 (N=2, same-outcome): `just status` and `just cost-audit` both defaulted to PROJ-001-animal-slots (shipped) while PROJ-002-machines-and-metrics was status:active; every PROJ-002 invocation needed an explicit ACTIVE_PROJECT=PROJ-002-machines-and-metrics or `just new-spec \"…\" STAGE-007 PROJ-002` override."
    status: addressed-in-instance
    disposition_at: project-close
    first_flagged: 2026-07-04
    resolved_note: >-
      Fixed in-instance (chore/reporting-polish): get_active_project now prefers the
      UNIQUE status:active project (reads project.status via a new get_project_status
      helper), falling back to lowest-numbered only when zero or several are active;
      ACTIVE_PROJECT still overrides. Plain `just status`/`backlog`/`roadmap`/`dash`/
      `cost-audit` now target the active wave (PROJ-002) with no env-var prefix; PROJ-001
      still reachable via ACTIVE_PROJECT. Bash 3.2 compatible (no mapfile). Upstream
      candidate — the template's resolver should ship this behavior by default.
    notes: >-
      Distinct from PROJ-001's `ambiguous-project-resolution` (which is about
      non-deterministic `find … | head -n1` when example+real projects coexist): here
      resolution is DETERMINISTIC but picks the wrong project because it sorts by id/dir
      name and never reads `status`. Fix options, cheapest first: (a) prefer the unique
      project with status:active, and only fall back to lowest-numbered when zero/many
      are active; (b) if multiple are active, hard-error listing them rather than
      silently picking one; (c) honor an ACTIVE_PROJECT env/`.active-project` file as the
      explicit override (already works as an undocumented escape hatch — promote + doc
      it). Same bug-CLASS as the PROJ-001 resolution/placeholder-lag cluster
      (ambiguous-project-resolution, repo-id-placeholder-lag, find-spec-matches-prompts-dir):
      a resolver quietly returns the wrong thing with no error. Belongs in the
      `silent-failure-tooling-cluster` risk. NOTE: `just specs-by-stage` is NOT affected —
      it deliberately iterates ALL projects and already shows PROJ-002 correctly; the
      status-blindness is confined to the single-active-project resolver.

  # ─────────────────────────── PRODUCT (what the template should build next) ───────────────────────────
  - id: roadmap-should-surface-planned-stages
    type: product
    summary: "roadmap / dash-future only rendered stages that had a stage FILE, so future stages the project already knows about (listed in the brief's Stage Plan) were invisible in the forward view until each was framed — the plan existed but reporting didn't show it."
    evidence: "PROJ-002 user request during STAGE-007 (N=1): with STAGE-007 framed and STAGE-008..011 still one-liners in the brief, `just roadmap` showed only STAGE-007 and `just backlog` said 'no upcoming stages', so multi-stage planning/reporting had no forward visibility beyond the active stage."
    status: addressed-in-instance
    disposition_at: project-close
    first_flagged: 2026-07-04
    notes: >-
      Fixed in-instance (chore/roadmap-planned-stages): roadmap.sh now reads the brief's
      "## Stage Plan" checkbox bullets, and renders any STAGE-NNN without a stage file as
      `planned / not yet framed` (+ its bolded title) in both the text and `--json`
      (`planned[]`) output; `dash future` inherits it. Only checkbox-bullet lines count
      (prose in the section that merely mentions a STAGE id is skipped), and stages that
      already have a file are filtered out (PROJ-001 regression: still 6 shipped rows, zero
      planned). Upstream candidate — the template's roadmap should surface the known-but-
      unframed arc by default so planning/reporting sees the whole project, not just what's
      been framed. Complements `active-project-resolver-status-blind` (now also
      addressed-in-instance): with the resolver fix, plain `just roadmap`/`dash future`
      show the active wave's full arc with no env-var prefix.

  - id: backlog-view-under-informative
    type: process-debt
    summary: "just backlog rendered each not-yet-specced bullet as only its first physical line (titles truncated mid-sentence, e.g. '… emoji /') with no [S/M/L] sizing, and its 'Upcoming stages' section listed framed stages only (counts) — so the planned arc a project already knows was largely invisible even pointed at the right project."
    evidence: "PROJ-002 user observation during STAGE-007 (N=1): `just backlog` for PROJ-002 showed 'SPEC-041 … emoji /' (cut at the source line-wrap), no sizing, and '(no upcoming stages)'."
    status: addressed-in-instance
    disposition_at: project-close
    first_flagged: 2026-07-04
    notes: >-
      Fixed in-instance (chore/reporting-polish): backlog.sh now (a) joins each bullet's
      wrapped continuation lines into one logical line before formatting (so the title +
      the trailing **[X]** sizing survive), (b) renders "SPEC-NNN  [X]  <bold title>" —
      the SPEC id, the S/M/L sizing, and the concise first-bold title; --json emits a clean
      {spec_id, title, complexity}, and (c) lists planned-but-unframed stages by name in an
      "Upcoming stages" section (framed → backlog count, planned → title), using the shared
      list_planned_stages parser now in _lib.sh (roadmap + backlog share one parser).
      Upstream candidate alongside roadmap-should-surface-planned-stages: the backlog view
      should show titles + sizing + the planned arc by default.

  # ─────────────────────────── LESSON (workflow patterns that worked / bit) ───────────────────────────
  - id: dead-config-field-caught-by-adversarial-mutation
    type: lesson
    summary: "A config field the engine never reads is dead config that looks live. MachineMath carries `reelWeights`, but no engine function consumes it — resolveGrid draws from the `REEL_STRIP` literal array. The design of the retune's measurement spec (SPEC-044) assumed weights drove hit-frequency; the adversarial guard-mutation is what exposed the field as inert."
    evidence: "PROJ-002 STAGE-008 SPEC-044 (N=1): the build agent's attempted adversarial check (bump REEL_WEIGHTS.WOLF, expect the pinned RTP baseline to move) moved NOTHING — the numbers only shift when REEL_STRIP or PAYTABLE change. Verify then proved teeth by mutating the real drivers (REEL_STRIP DEER→WOLF moved rtp 0.1295→0.1254; PAYTABLE.low[2] 5→50 moved it →0.3005), both reverted clean."
    status: noted
    disposition_at: stage-close
    first_flagged: 2026-07-05
    notes: >-
      Two reusable takeaways. (1) The "revert-and-confirm-the-guard-fails" adversarial move
      (promoted to default verify step in STAGE-007) does double duty: it not only proves a
      guard has teeth, it MAPS which inputs actually drive behavior — here it caught a config
      field that the config-driven-machine model (DEC-015) exposes but the engine ignores.
      (2) For STAGE-008's retune (SPEC-045), retuning hit-frequency/RTP means editing the
      STRIP composition and/or PAYTABLE, NOT `reelWeights`; open design question for the
      retune DEC — either drop `reelWeights` from MachineMath, or make `strips` GENERATED
      from `reelWeights` so the weights become a live, determinism-affecting tuning knob.
      Template angle: a config-object refactor should assert (a lint/test) that every field
      of the config is consumed somewhere, or dead fields accrete as misleading documentation.

  - id: measure-before-tune-during-design
    type: lesson
    summary: "Running the real measurement DURING design (not just specifying it) turned the retune's baseline from an invented fixture into a reproduced fact, so the build reproduced it first-try with zero iteration."
    evidence: "PROJ-002 STAGE-008 SPEC-044 (N=1): the orchestrator wrote a throwaway simulator matching the exact spec'd algorithm and ran it via vite-node during DESIGN, pinning W&W's measured baseline (rtp 0.1295 / hitFreq 0.0999 / tierCounts {none:45003,small:4786,big:211,jackpot:0}). The build agent's implementation reproduced the pin bit-for-bit on the first `just test` run."
    status: noted
    disposition_at: stage-close
    first_flagged: 2026-07-05
    notes: >-
      Generalizes the STAGE-007 "recompute-then-pin" discipline to NEW measurements: when a
      spec pins a computed baseline, the designer computes it with the exact algorithm the
      spec dictates (here vite-node ran the drop-in verbatim), so the failing-test value is a
      reproduced fact, not a guess the build must reverse-engineer. Precondition: the spec's
      algorithm must be pinned precisely enough (seed derivation, call shape, constants) that
      the design-time and build-time runs are bit-identical — which they were. Also surfaced
      the retune's headline "before" number as a side effect (RTP ~13% — the quantified
      "too hard to win").

  - id: adversarial-mutation-must-be-behavior-distinguishing
    type: lesson
    summary: "An adversarial guard-mutation only has teeth if some observation can distinguish the mutation from the original. Three sub-cases: (1) mutating code redundant with a language guarantee (e.g. ES2019 stable sort) is a no-op no test can catch — document, don't 'fix'; (2) a real behavioral difference the LITERAL test text doesn't yet pin — add the pinning assertion; (3) a difference not observable via RETURN VALUE under current data (e.g. a reactive seam with one registered item) — pin it structurally with a delegation/spy test. For a 'no-op seam' spec, author the teeth test in Failing Tests up front."
    evidence: "PROJ-002 STAGE-008 SPEC-045 + SPEC-048 + SPEC-049 (N=3). SPEC-045 (case 1): removing buildStrip's `|| a.ord - b.ord` tie-break failed 0/320 tests — Array.sort is stable (ES2019+) and items are inserted in `symbols` order, so the tie-break is provably redundant; kept it (defensive) + documented. SPEC-048 (case 2): the design named three mutations but two (getChannel's active-gain read; applyMix's active-bed restore) weren't distinguished by the literal test text — the build agent added one assertion each so the mutations break a test. SPEC-049 (case 3): mutation (c) 'getActiveMachine ignores storage, returns const default' broke no test, because the persisted-id fixture ('wild-and-whimsical') EQUALS DEFAULT_MACHINE_ID in the single-machine registry — no return-value assertion can distinguish read-storage from const-default until a 2nd machine exists (SPEC-051). Verify closed it with a `vi.spyOn` delegation test ('getActiveMachine delegates to readActiveMachineId') that passes on correct code and fails under the mutation. Fix across all three: before prescribing 'revert X → test Y fails', confirm SOME observation distinguishes X — a pinned value (case 2) or a structural spy (case 3) — and author that test first; if none can (case 1), it's redundant code, not a test gap."
    status: noted
    disposition_at: stage-close
    first_flagged: 2026-07-05
    notes: >-
      Refines the STAGE-007 "revert-and-confirm-the-guard-fails" default verify move: before
      prescribing (or trusting) an adversarial mutation, confirm the mutated line is
      behavior-distinguishing — i.e. there EXISTS an input for which the original and mutated
      code produce different observable output. If no such input can be constructed, the code is
      provably redundant (often with a language/library guarantee), which is a code-quality note
      (document or delete the redundancy), NOT a test-strength defect. Practical test for the
      designer: when writing a "mutate X and confirm a test fails" check into a spec's Notes,
      sanity-run the mutation once during design so the spec never prescribes a no-op probe. Also
      a template angle: verify agents should distinguish "guard has no teeth (add a test)" from
      "mutation has no observable effect (redundant code)" — different remedies.

  # ─────────────────────────── PROCESS-DEBT (in-flight friction) ───────────────────────────
  - id: subagent-metering-truncated-by-session-limit
    type: process-debt
    summary: "When a build/verify sub-agent's session hits the 5-hour usage-window limit mid-run, the Agent result's subagent_tokens can come back truncated/implausible (662 tok for 40 tool-uses + a full cold verify), so the orchestrator's cost bookkeeping (which fills tokens_total from subagent_tokens) records a large undercount."
    evidence: "PROJ-002 STAGE-008 SPEC-045 verify (N=1): the verify sub-agent committed its bookkeeping, then its final message was the session-limit error; usage reported subagent_tokens=662 (impossible for the work done) and duration_ms=4355174 (72min, mostly idle wait on the reset). Recorded honestly with a caveat field rather than fabricated; SPEC-045 totals therefore undercount verify."
    status: noted
    disposition_at: project-close
    first_flagged: 2026-07-05
    notes: >-
      Not a template bug per se, but a cost-integrity gap the orchestration convention should
      name: if a sub-agent terminates on a rate/session limit, treat its reported subagent_tokens
      as unreliable — record it with an explicit caveat (done here via a `caveat:` field on the
      cost session) and do NOT let it silently deflate the spec/stage/project cost totals. Options
      for the template: (a) a cost-audit heuristic that flags implausibly-low metered cycles
      (tokens/tool_use below a floor) for manual review; (b) an orchestrator convention to
      re-estimate from a comparable prior cycle when metering is known-truncated, clearly marked
      as an estimate. cost-audit only checks non-null, so the undercount passes silently today.

  - id: behavior-change-blast-radius-includes-ui-fixtures
    type: lesson
    summary: "When a behavior-changing spec edits an engine constant that the UI also renders, the fixture blast radius spans BOTH the engine tests AND the UI tests (a UI test can pin a payout string, an aria label, or a count derived from the changed data). Designing the re-baseline from the engine side alone under-counts it."
    evidence: "PROJ-002 STAGE-008 SPEC-046 (N=1): the retune (weights/paytable/paylines 5→20) was designed with all 8 ENGINE-side fixtures pinned, but the build agent had to re-derive 4 more UI fixtures the change also moved — paytable.test.ts (multipliers), PaytableSheet.test.tsx (payout text /200/→/250/), PaylineMap.test.tsx (aria label from the now-20 lines), spin.test.ts (strip length/stops). All caught by the gate + the spec's explicit 'unlisted-but-touched fixtures may need re-derivation' clause."
    status: noted
    disposition_at: stage-close
    first_flagged: 2026-07-05
    notes: >-
      Two takeaways. (1) Design-time blast-radius survey for a behavior-changing spec should grep
      the WHOLE test tree (engine + UI) for the changing constants/types, not just the engine — a
      LineId type widening or a paytable retune ripples into any UI test that renders those values.
      (2) The mitigation that worked: pin the exact values you CAN compute (the engine contract +
      metrics), and explicitly license the build to re-derive unlisted-but-touched fixtures against
      the deterministic engine, with the gate as the completeness enforcer. This "hard pins +
      licensed re-derivation" pattern is the right template for wide re-baselines — reproduces
      SPEC-044/045's measure-then-pin at larger scale. Complements
      [[adversarial-mutation-must-be-behavior-distinguishing]] and the STAGE-007 re-baseline notes.
  - id: drop-in-test-literals-should-be-tsc-checked
    type: lesson
    summary: "When a spec ships verbatim drop-in code for a TEST that builds an inline object literal for a strongly-typed field, type-check that literal against strict tsc at design time — tuple-typed fields (readonly [number, number, number]) need `as const` on array literals or the build hits a tsc error and must patch the 'verbatim' code."
    evidence: "PROJ-002 STAGE-008 SPEC-047 (N=1): the design's stub-math test literal `paytable: { low: [9,9,9], ... }` widened to `number[]` and failed `tsc --strict` against `MachineMath.paytable`'s `readonly [number, number, number]`; the build agent added `as const` to each tuple (runtime unchanged). The only deviation in an otherwise pure-transcription build."
    status: noted
    disposition_at: stage-close
    first_flagged: 2026-07-06
    notes: >-
      Small but recurring: the "measure-then-pin, build-is-transcription" discipline
      ([[measure-before-tune-during-design]]) extends to TYPE as well as VALUE. Source drop-ins
      already get compiled during design (they're pinned against the real engine); test-file
      drop-ins that construct typed literals should get the same treatment — a quick `vite-node`/
      `tsc` pass on the literal, or just pre-emptively annotate tuple literals `as const`. Cheap to
      prevent, and it keeps "implement verbatim" honestly verbatim. Not worth a template change on
      N=1; revisit if another parameterization spec repeats it.
```

## meta

**Multi-project is the trigger.** Every PROJ-001 resolution bug was masked while only
one real project existed. The moment PROJ-002 opened as a second, *active* wave beside a
*shipped* PROJ-001, the status-blind resolver started pointing every default-scoped
command at the wrong (finished) project. This is the concrete cost of the resolution
cluster the PROJ-001 retrospective flagged: it only bites once a repo actually holds
more than one project — exactly the state a "continuous, multi-wave" repo is designed to
reach. The orchestrator worked around it all session with explicit `ACTIVE_PROJECT=…` /
`PROJ-002` args, but a fresh-session user following the template verbatim would read
stale PROJ-001 status and a green cost-audit that never even inspected the active wave.
